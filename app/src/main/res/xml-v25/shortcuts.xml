<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">

    <shortcut
        android:shortcutId="sc_files"
        android:enabled="true"
        android:icon="@drawable/qc_files"
        android:shortcutShortLabel="@string/album_title_files"
        android:shortcutLongLabel="@string/album_title_files"
        android:shortcutDisabledMessage="@string/album_title_files">
        <intent
            android:action="sc_files"
            android:targetPackage="de.baumann.browser"
            android:targetClass="de.baumann.browser.Activity.BrowserActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the fragments is what the user sees when they
             launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

    <shortcut
        android:shortcutId="sc_login"
        android:enabled="true"
        android:icon="@drawable/qc_pass"
        android:shortcutShortLabel="@string/album_title_pass"
        android:shortcutLongLabel="@string/album_title_pass"
        android:shortcutDisabledMessage="@string/album_title_pass">
        <intent
            android:action="sc_login"
            android:targetPackage="de.baumann.browser"
            android:targetClass="de.baumann.browser.Activity.BrowserActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the fragments is what the user sees when they
             launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

    <shortcut
        android:shortcutId="sc_bookmark"
        android:enabled="true"
        android:icon="@drawable/qc_bookmarks"
        android:shortcutShortLabel="@string/album_title_bookmarks"
        android:shortcutLongLabel="@string/album_title_bookmarks"
        android:shortcutDisabledMessage="@string/album_title_bookmarks">
        <intent
            android:action="sc_bookmark"
            android:targetPackage="de.baumann.browser"
            android:targetClass="de.baumann.browser.Activity.BrowserActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the fragments is what the user sees when they
             launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

    <shortcut
        android:shortcutId="sc_history"
        android:enabled="true"
        android:icon="@drawable/qc_history"
        android:shortcutShortLabel="@string/album_title_history"
        android:shortcutLongLabel="@string/album_title_history"
        android:shortcutDisabledMessage="@string/album_title_history">
        <intent
            android:action="sc_history"
            android:targetPackage="de.baumann.browser"
            android:targetClass="de.baumann.browser.Activity.BrowserActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the fragments is what the user sees when they
             launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
    </shortcut>

</shortcuts>
        


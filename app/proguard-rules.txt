-dontwarn org.xmlpull.v1.**
-keep class org.xmlpull.v1.** {*;}

-dontwarn android.content.res.**
# kxml2's META-INF/services/org.xmlpull.v1.XmlPullParserFactory lists parser and
# serializer as a comma-separated entry that R8 parses literally as a missing class.
-dontwarn org.kxml2.io.**

-keep class org.xmlpull.** { *; }
-keepclassmembers class org.xmlpull.** { *; }

-assumenosideeffects class android.util.Log {
    public static boolean isLoggable(java.lang.String, int);
    public static int v(...);
    public static int d(...);
    public static int i(...);
}

-keepnames class <1>$$serializer {static <1>$$serializer INSTANCE;}

# BrowserAction subclass simple-names are used as stable IDs persisted in
# SharedPreferences (gesture bindings).  R8 must not rename them.
-keepnames class info.plateaukao.einkbro.browser.BrowserAction$* { }